<?xml version="1.0" encoding="utf-8"?>

<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <group android:checkableBehavior="single">
        <item android:id="@+id/grid_view_type"
            android:title="Search"
            android:icon="@drawable/ic_grid_view"
            android:tint="#FFFFFF"
            android:checked="true"
            app:showAsAction="ifRoom|collapseActionView" />
        <item android:id="@+id/staggered_grid_view_type"
            android:title="Search"
            android:icon="@drawable/ic_staggered_grid_view"
            android:tint="#FFFFFF"
            app:showAsAction="ifRoom|collapseActionView" />
        <item android:id="@+id/list_view_type"
            android:title="Search"
            android:icon="@drawable/ic_list_view"
            android:tint="#FFFFFF"
            app:showAsAction="ifRoom|collapseActionView" />
    </group>
</menu>